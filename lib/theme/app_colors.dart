import 'package:flutter/material.dart';

class AppColors {
  // Fundo
  static const bgDark = Color(0xFF071522);
  static const bgPanel = Color(0xFF102637);
  static const bgPanelAlt = Color(0xFF102637);
  static const bgField = Color(0xFF071B2C);

  // Bordas
  static const border = Color(0xFF294E6B);
  static const borderHover = Color(0xFF467C9D);

  // Texto
  static const textPrimary = Color(0xFFE6F4FF);
  static const textSecondary = Color(0xFFD6ECFB);
  static const textMuted = Color(0xFF9AB9CD);
  static const textFaint = Color(0xFF7197B0);

  // Acentos
  static const accentBlue = Color(0xFF217BA5);
  static const accentBlueLight = Color(0xFF38BDF8);
  static const accentCyan = Color(0xFF67D5FF);

  // Estados
  static const emergencyRed = Color(0xFFE5484D);
  static const successGreen = Color(0xFF4ADE80);
  static const successGreenDark = Color(0xFF2A9876);
  static const successGreenBg = Color(0xFF062A1B);
}
